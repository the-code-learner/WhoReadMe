{
  "name": "@who-read-me/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name whoreadme",
    "dev": "npx wrangler pages dev dist --port 4173"
  }
}
